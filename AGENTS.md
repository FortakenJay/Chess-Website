# Agent instructions

This is **LEAK**, a Chess.com training app (TanStack Start + Supabase + Stockfish WASM).

Before changing product code, read:

1. `docs/llm/INDEX.md`
2. `docs/llm/SYSTEM.md`
3. The `docs/llm/_*.md` file for the domain you are touching

Human diagrams: `docs/humans/`. Do not rewrite those unless the architecture actually changed.

Enforced decisions while coding: `.cursor/rules/*.mdc`. Keep the vault, the rule, and the code in sync.
